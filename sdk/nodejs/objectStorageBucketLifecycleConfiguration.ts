// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Lifecycle configurations automate object management by defining actions applied to objects over time, such as expiring objects after a specified period or transitioning them to different storage tiers. This helps optimize storage costs and maintain data hygiene. Lifecycle configurations automate object management by defining actions applied to objects over time, such as expiring objects after a specified period or transitioning them to different storage tiers. This helps optimize storage costs and maintain data hygiene. [Learn more about S3-compatible lifecycle bucket configurations](https://docs.coreweave.com/products/storage/object-storage/reference/object-storage-s3#bucket-lifecycles).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as coreweave from "@pulumi/coreweave";
 *
 * const _default = new coreweave.ObjectStorageBucket("default", {
 *     name: "bucket-lifecycle-example",
 *     zone: "US-EAST-04A",
 * });
 * const defaultObjectStorageBucketVersioning = new coreweave.ObjectStorageBucketVersioning("default", {
 *     bucket: _default.name,
 *     versioningConfiguration: {
 *         status: "Enabled",
 *     },
 * });
 * const defaultObjectStorageBucketLifecycleConfiguration = new coreweave.ObjectStorageBucketLifecycleConfiguration("default", {
 *     bucket: _default.name,
 *     rules: [
 *         {
 *             id: "cleanup-logs",
 *             status: "Enabled",
 *             filter: {
 *                 and: {
 *                     prefix: "logs/",
 *                     objectSizeGreaterThan: 1000000,
 *                     tags: {
 *                         env: "prod",
 *                     },
 *                 },
 *             },
 *             expiration: {
 *                 days: 30,
 *             },
 *             noncurrentVersionExpiration: {
 *                 noncurrentDays: 7,
 *                 newerNoncurrentVersions: 2,
 *             },
 *         },
 *         {
 *             id: "expire-traces",
 *             prefix: "traces/",
 *             status: "Enabled",
 *             abortIncompleteMultipartUpload: {
 *                 daysAfterInitiation: 5,
 *             },
 *             expiration: {
 *                 date: "2026-01-01T00:00:00Z",
 *             },
 *         },
 *     ],
 * }, {
 *     dependsOn: [defaultObjectStorageBucketVersioning],
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import coreweave:index/objectStorageBucketLifecycleConfiguration:ObjectStorageBucketLifecycleConfiguration default {{bucket_name}}
 * ```
 */
export class ObjectStorageBucketLifecycleConfiguration extends pulumi.CustomResource {
    /**
     * Get an existing ObjectStorageBucketLifecycleConfiguration resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ObjectStorageBucketLifecycleConfigurationState, opts?: pulumi.CustomResourceOptions): ObjectStorageBucketLifecycleConfiguration {
        return new ObjectStorageBucketLifecycleConfiguration(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'coreweave:index/objectStorageBucketLifecycleConfiguration:ObjectStorageBucketLifecycleConfiguration';

    /**
     * Returns true if the given object is an instance of ObjectStorageBucketLifecycleConfiguration.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ObjectStorageBucketLifecycleConfiguration {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ObjectStorageBucketLifecycleConfiguration.__pulumiType;
    }

    /**
     * Name of the bucket to apply lifecycle configuration to
     */
    declare public readonly bucket: pulumi.Output<string>;
    /**
     * One or more lifecycle rule blocks
     */
    declare public readonly rules: pulumi.Output<outputs.ObjectStorageBucketLifecycleConfigurationRule[] | undefined>;

    /**
     * Create a ObjectStorageBucketLifecycleConfiguration resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ObjectStorageBucketLifecycleConfigurationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ObjectStorageBucketLifecycleConfigurationArgs | ObjectStorageBucketLifecycleConfigurationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ObjectStorageBucketLifecycleConfigurationState | undefined;
            resourceInputs["bucket"] = state?.bucket;
            resourceInputs["rules"] = state?.rules;
        } else {
            const args = argsOrState as ObjectStorageBucketLifecycleConfigurationArgs | undefined;
            if (args?.bucket === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            resourceInputs["bucket"] = args?.bucket;
            resourceInputs["rules"] = args?.rules;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ObjectStorageBucketLifecycleConfiguration.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ObjectStorageBucketLifecycleConfiguration resources.
 */
export interface ObjectStorageBucketLifecycleConfigurationState {
    /**
     * Name of the bucket to apply lifecycle configuration to
     */
    bucket?: pulumi.Input<string>;
    /**
     * One or more lifecycle rule blocks
     */
    rules?: pulumi.Input<pulumi.Input<inputs.ObjectStorageBucketLifecycleConfigurationRule>[]>;
}

/**
 * The set of arguments for constructing a ObjectStorageBucketLifecycleConfiguration resource.
 */
export interface ObjectStorageBucketLifecycleConfigurationArgs {
    /**
     * Name of the bucket to apply lifecycle configuration to
     */
    bucket: pulumi.Input<string>;
    /**
     * One or more lifecycle rule blocks
     */
    rules?: pulumi.Input<pulumi.Input<inputs.ObjectStorageBucketLifecycleConfigurationRule>[]>;
}
